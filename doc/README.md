# STARTPRO easy-website 项目技术文档

> 面向开发 / 维护者的中文技术文档。本文档只描述代码与配置现状，不含业务操作手册。

## 项目一句话简介

前后端分离的企业官网建站系统：后台通过**可视化拖拽 PageBuilder** 编排页面组件、配置主题与站点资料、管理文档与通知；前台按 slug 渲染发布的页面。前端 Next.js 运行在 **3001** 端口，后端 Express API 运行在 **3003** 端口，数据库为 **MySQL**。

## 文档目录

| 编号 | 文档 | 一句话说明 | 适合读者 |
| --- | --- | --- | --- |
| — | [README.md](./README.md) | 本文档、项目概览与文档导航 | 所有人 |
| 01 | [01-项目概述.md](./01-项目概述.md) | 产品定位、功能模块、技术栈、主干架构图 | 评审 / 接手 |
| 02 | [02-技术架构与代码结构.md](./02-技术架构与代码结构.md) | 前后端目录树、关键文件职责、核心流程、已知技术债 | 后端 / 全栈开发 |
| 03 | [03-数据库设计.md](./03-数据库设计.md) | 20 张表结构与关系、多语言列、语言表与一次性对齐说明 | 后端 / DBA |
| 04 | [04-后端接口文档.md](./04-后端接口文档.md) | 15 个路由模块端点清单、响应与鉴权约定 | 前后端联调 |
| 05 | [05-前端页面构建器与组件.md](./05-前端页面构建器与组件.md) | PageBuilder 架构、30+ 组件、主题/字体系统、文档站 | 前端开发 |
| 06 | [06-部署与运维指南.md](./06-部署与运维指南.md) | 环境要求、.env 字段、init.sql 导入、nginx 双配置、上线步骤 | 运维 / 部署 |
| 07 | [07-浏览器缓存与弱网回退.md](./07-浏览器缓存与弱网回退.md) | 前端数据缓存/版本接口、图片 SW 缓存、语言缓存与守卫、切页预取 | 前后端开发 |

## 速查入口

- **产品与技术栈速览** → [01](./01-项目概述.md)
- **接手开发、改功能** → [02](./02-技术架构与代码结构.md) → [03](./03-数据库设计.md) → [04](./04-后端接口文档.md)
- **前端页面/组件开发** → [05](./05-前端页面构建器与组件.md)
- **从零部署上线** → [06](./06-部署与运维指南.md)
- **弱网/离线缓存与浏览器缓存体系** → [07](./07-浏览器缓存与弱网回退.md)

## 关键事实速记

- 库名 `pailink`；`init.sql` 现为纯建表（无 seed），后台管理员账号需首次自行创建。
- 端口约定：前端 3001、后端 3003、后台管理信令端口 23080（nginx，仅用于暴露后台入口）。
- 后台路径 `/admin` 在前台 nginx 中被 `deny all` 屏蔽，后台通过独立端口 23080 访问。
- 静态资源路径：`/uploads`（用户上传）、`/system-default`（内置素材）、`/ck-umd`（CKEditor 前端静态资源）。
- 前台存在浏览器缓存体系（弱网回退）：版本接口 `GET /api/content/status`、缓存工具 `utils/dataCache.ts`(SWR)、图片 Service Worker `public/sw.js`、弱网横幅 `StaleDataBanner`，详见 [07](./07-浏览器缓存与弱网回退.md)。

### 后台上账号与登录（2026-09 新增）

后台登录采用 **Keycloak SSO（OIDC 授权码）+ 本地账号兜底** 的双通道方案，Wiki.js 风格的方法实例化管理：

- **登录方法**：`local`（本地账号密码，固定保留、不可停用/删除）为默认；Keycloak 方法可动态新增（「登录管理」页左侧「新增登录方法」下拉框），连接配置（`ISSUER_URL` / `CLIENT_ID` / `CLIENT_SECRET`）在页面填写存库，**改完即时生效**。
- **登录管理页** `/admin/auth`（仅 admin）：左侧方法列表 + 右侧配置面板（显示名、排序、启用开关、自动创建账号 + 自动分配角色组）。已启用且配置完整的方法才出现在登录页。
- **账号认领/建户**：keycloak 方法开启「登录时自动创建账号」并指定角色组后，首登自动建户（按 `keycloak_id` → `email` 认领 → 建户）；关闭时仅已登记账号可登录、不覆盖本地角色。
- **用户管理页** `/admin/users`（仅 admin）：受「本地登录-创建账号」开关控制——开即支持增删改查与角色分配，关则只读 + 改姓名。
- 本地账号密码登录保留为 Keycloak 故障降级通道；Express `authenticateToken` 鉴权中间件与前端 401 refresh 机制不变。
- 关键实现：`backend/src/utils/{authMethods,keycloakClient}.js`、`backend/src/routes/{auth-methods,keycloak,users}.js`、`frontend/src/pages/admin/auth.tsx`；接口清单见 [04](./04-后端接口文档.md)。

> `init.sql` 已移除旧模板 seed 数据与临时视图工件，简化为纯建表 + 幂等多语言迁移；旧配置服务器运行一次即可对齐且不覆写数据（见 [03](./03-数据库设计.md#6-多语言增量迁移一次性对齐旧配置)）。